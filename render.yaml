services:
  - type: web
    name: mediastance-api
    runtime: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements_hf.txt
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: HF_TOKEN
        sync: false          # set manually in Render dashboard
      - key: HF_DATASET_REPO
        value: JhanviN/mediastance-db
      - key: HF_MODEL_REPO
        value: JhanviN/mediastance-deploy
      - key: SYNC_INTERVAL_MINUTES
        value: "30"
      - key: PIPELINE_INTERVAL_MINUTES
        value: "0"           # disable in-process pipeline — GitHub Actions handles it
      - key: PYTHON_VERSION
        value: "3.11.0"
